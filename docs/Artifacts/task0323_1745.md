created: 2026-03-23 18:00 (JST)
author: AI Agent (Gemini 2.0 Pro)

# Task List: Poetryからuvへの移行

- [x] `pyproject.toml` を標準の `[project]` メタデータ形式に書き換え
- [x] 不要ファイルの削除 (`poetry.lock`, `.venv/`)
- [x] `uv sync` コマンドによる新しい仮想環境と依存関係の構築（`uv.lock`の生成）
- [x] `README.md` のコマンド表記更新 (`poetry` -> `uv`)
- [x] (Verification) `uv run TokenCalc .` 等が正常に動作するかテスト実行
